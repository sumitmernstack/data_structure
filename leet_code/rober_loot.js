let house=[2,7,3,1,4,2,1,8]

function rober(arr) {
    let dp=new Array(n+1).fill(0)
    dp[0]=arr[0]
    dp[1]=Math.max(arr[0],arr[1])

    for (let i = 2; i < arr.length; i++){
        
     
    }

}